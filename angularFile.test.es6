import angularFile from 'examples/angularFile' 

describe('angularFile', () => {
  beforeEach(() => {
    // prep here
  })

  describe('fetchVersionsAndRevisions()', () => {
    beforeEach(() => {
      // prep here
    })

    it('does something', () => {
      expect()
    })
  })

  describe('revertVersion()', () => {
    beforeEach(() => {
      // prep here
    })

    it('does something', () => {
      expect()
    })
  })

  describe('selectVersionOrRevision()', () => {
    beforeEach(() => {
      // prep here
    })

    it('does something', () => {
      expect()
    })
  })

  describe('revertRevision()', () => {
    beforeEach(() => {
      // prep here
    })

    it('does something', () => {
      expect()
    })
  })

  describe('revertVersion()', () => {
    beforeEach(() => {
      // prep here
    })

    it('does something', () => {
      expect()
    })
  })

  describe('toggle()', () => {
    beforeEach(() => {
      // prep here
    })

    it('does something', () => {
      expect()
    })
  })
})
